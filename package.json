{
  "name": "keel-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive simulator comparing a SAFE-only seed fund with the same fund using Keel SAFE + Option protected positions.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
