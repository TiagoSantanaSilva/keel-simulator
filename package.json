{
  "name": "keel-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive simulator comparing a traditional seed fund, a Keel protected-position seed fund, and a fund that waits for the Series A.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
